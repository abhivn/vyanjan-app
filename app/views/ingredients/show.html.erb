<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @ingredient.name %>
</p>

<% if can? :update, @ingredient %> <%= link_to 'Edit', edit_ingredient_path(@ingredient) %> | <% end %> 
<% if can? :destroy, @ingredient %> <%= link_to 'Back', ingredients_path %> <% end %>
